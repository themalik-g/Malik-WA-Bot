// Optional: handle presence updates
function handlePresenceUpdate(update) {
    // You can log or react to presence changes here if needed
    // Example: console.log('Presence update:', update);
    // For now, do nothing
}

module.exports = {
    handlePresenceUpdate
};
